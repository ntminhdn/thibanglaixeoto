<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.minhnt.thibanglaixeoto">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".learn.LearnActivity"
            android:label="Học" />
        <activity
            android:name=".practice.PracticeActivity"
            android:label="Ôn Tập" />
        <activity android:name=".exam.ExamActivity" />
        <activity android:name=".exam.ChooseExamActivity" />
        <activity android:name=".ramdom.RandomActivity"></activity>
    </application>

</manifest>