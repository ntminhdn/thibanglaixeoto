<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.minhnt.thibanglaixeoto">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <application
        android:allowBackup="true"
        android:hardwareAccelerated="true"
        android:icon="@drawable/app_store"
        android:label="@string/app_name"
        android:roundIcon="@drawable/app_store"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".learn.LearnActivity"
            android:label="Học" />
        <activity
            android:name=".practice.PracticeActivity"
            android:label="Ôn Tập" />
        <activity
            android:name=".exam.ExamActivity"
            android:label="Thi thử" />
        <activity
            android:name=".exam.ChooseExamActivity"
            android:label="Chọn đề thi" />
        <activity
            android:name=".ramdom.RandomActivity"
            android:label="Kiểm tra" />
        <activity
            android:name=".exam.CongratActivity"
            android:label="Chúc mừng">

        </activity>
    </application>

</manifest>